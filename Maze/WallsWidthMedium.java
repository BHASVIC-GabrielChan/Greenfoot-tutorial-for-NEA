import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WallsWidthMedium here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WallsWidthMedium extends Walls
{
    /**
     * Act - do whatever the WallsWidthMedium wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public WallsWidthMedium(){
    getImage().scale(getImage().getWidth() * 4, getImage().getHeight());}
    public void act()
    {
        // Add your action code here.
    }
}
