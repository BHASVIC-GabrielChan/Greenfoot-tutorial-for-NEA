import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WallsHeightMedium here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WallsHeightMedium extends Walls
{
    /**
     * Act - do whatever the WallsHeightMedium wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public WallsHeightMedium()
    {getImage().scale(getImage().getWidth() / 2, getImage().getHeight() *8);}
    public void act()
    {
        // Add your action code here.
    }
}
