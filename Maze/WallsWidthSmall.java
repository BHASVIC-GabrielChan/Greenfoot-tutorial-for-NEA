import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class WallsWidthSmall here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WallsWidthSmall extends Walls
{
    /**
     * Act - do whatever the WallsWidthSmall wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public WallsWidthSmall()
    {getImage().scale(getImage().getWidth() * 2, getImage().getHeight());}
    public void act()
    {
        // Add your action code here.
    }
}
